use reqwest::{Client, Url};
// use crate::error::ClientError;
// use crate::models::{ApiResponse, Site, Device};


impl UniFiClient {
    pub fn new(gateway_url: &str) -> Result<Self> {
        let gateway_url = gateway_url;
    }
}